Cloning : git clone "url:"

Status : These help to find a status of Git Files

ChangeBranch: git branch -m "Brnach name!!"

init : to create a negot repo   THESE Command is basic for push and pull repo

git remote add origin <--link--> : remote to set a link to remote

gir remote -v : to check a status of remote branch which LINK

git branch : to check which is current brnach

git beanch -M main : to change a Main branch to main
